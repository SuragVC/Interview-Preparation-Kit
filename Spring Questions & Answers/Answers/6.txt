Q. Why we should avoid the spring framework?
	SpringBoot unnecessarily increases the size of the build with unused dependencies. It's only suitable for micro 
services which eventually need to deploy in docker but not for the mono lithics web services.